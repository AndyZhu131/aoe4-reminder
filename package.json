{
  "name": "aoe4-reminder-workspace",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix src/frontend run dev",
    "build:backend": "npm --prefix src/frontend run build:backend",
    "dist": "npm --prefix src/frontend run dist"
  }
}
